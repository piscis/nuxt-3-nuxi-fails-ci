{
  "name": "nuxt3-tests-fails",
  "private": true,
  "scripts": {
    "clean": "rimraf node_modules .nuxt .output dist",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "start": "node .output/server/index.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "nuxi test",
    "test:watch": "nuxi test --watch --dev",
    "test:ci": "nuxi test; echo $?"
  },
  "devDependencies": {
    "nuxt": "^3.2.0",
    "@nuxt/test-utils": "^3.2.0",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.5",
    "vitest": "^0.28.4"
  }
}
